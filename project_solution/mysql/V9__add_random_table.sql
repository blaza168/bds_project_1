-- Random table to fit requirements. Notes.
CREATE TABLE notes (
                       note_id     INT NOT NULL auto_increment PRIMARY KEY,
                       content     TEXT NOT NULL
) DEFAULT CHARACTER SET 'UTF8';